NEXT_PUBLIC_API_URL=http://localhost:8000

# Capacitor only: point the mobile shell at a live Next dev server for hot-reload
# instead of the bundled static export. Leave unset to use the bundled build.
# CAPACITOR_DEV_SERVER_URL=http://<your-lan-ip>:3003
